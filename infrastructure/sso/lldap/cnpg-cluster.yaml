---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: lldap-db
  namespace: authelia
spec:
  instances: 1
  storage:
    storageClass: ceph-block
    size: 1Gi
  walStorage:
    storageClass: ceph-block
    size: 1Gi
  bootstrap:
    initdb:
      database: lldap
      owner: lldap
  backup:
    retentionPolicy: "30d"
    target: prefer-standby
    barmanObjectStore:
      destinationPath: s3://cnpg-lldap/patch/lldap/cnpg
      endpointURL: https://rustfs.internal.stplabs.io
      serverName: lldap-db
      s3Credentials:
        accessKeyId:
          name: lldap-db-backup-s3-creds
          key: ACCESS_KEY_ID
        secretAccessKey:
          name: lldap-db-backup-s3-creds
          key: ACCESS_SECRET_KEY
      data:
        compression: bzip2
        jobs: 2
      wal:
        compression: bzip2
  monitoring:
    enablePodMonitor: true
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      memory: 256Mi
