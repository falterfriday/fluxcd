---
apiVersion: v1
kind: ConfigMap
metadata:
  name: authelia-theme-proxy
  namespace: authelia
data:
  nginx.conf: |
    worker_processes auto;
    pid /tmp/nginx.pid;
    events { worker_connections 1024; }
    http {
      include /etc/nginx/mime.types;
      default_type application/octet-stream;
      client_body_temp_path /tmp/client_temp;
      proxy_temp_path /tmp/proxy_temp;
      fastcgi_temp_path /tmp/fastcgi_temp;
      uwsgi_temp_path /tmp/uwsgi_temp;
      scgi_temp_path /tmp/scgi_temp;
      access_log /dev/stdout;
      error_log /dev/stderr warn;

      server {
        listen 8080;
        server_name _;
        location = /themeproxy/custom.css {
          default_type text/css;
          return 200 'a[href*="authelia.com"]{display:none !important;}';
        }
        location = /themeproxy/theme.css {
          default_type text/css;
          alias /themeproxy-assets/theme.css;
        }

        location / {
          resolver 10.43.0.10 valid=30s ipv6=off;
          set $authelia_upstream authelia.authelia.svc.cluster.local;
          proxy_pass http://$authelia_upstream:80$request_uri;
          proxy_http_version 1.1;
          proxy_set_header Host $host;
          proxy_set_header X-Forwarded-Proto https;
          proxy_set_header X-Forwarded-Host $host;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header Accept-Encoding "";
          proxy_redirect off;

          sub_filter_types text/html;
          sub_filter_once on;
          sub_filter '</head>' '<link rel="stylesheet" type="text/css" href="/themeproxy/theme.css"><link rel="stylesheet" type="text/css" href="/themeproxy/custom.css"></head>';
        }
      }
    }

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: authelia-theme
  namespace: authelia
data:
  theme.css: |
    @import url("https://theme-park.dev/css/base/authelia/aquamarine.css");
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: authelia-theme-proxy
  namespace: authelia
  labels:
    app.kubernetes.io/name: authelia-theme-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: authelia-theme-proxy
  template:
    metadata:
      labels:
        app.kubernetes.io/name: authelia-theme-proxy
    spec:
      containers:
        - name: nginx
          image: nginxinc/nginx-unprivileged:1.27-alpine
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              memory: 64Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: nginx-conf
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
              readOnly: true
            - name: theme
              mountPath: /themeproxy-assets
              readOnly: true
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: nginx-conf
          configMap:
            name: authelia-theme-proxy
        - name: theme
          configMap:
            name: authelia-theme
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: authelia-theme-proxy
  namespace: authelia
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: authelia-theme-proxy
  ports:
    - name: http
      port: 80
      targetPort: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: authelia-theme-proxy
  namespace: authelia
  annotations:
    cert-manager.io/cluster-issuer: cloudflare-cluster-issuer
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  ingressClassName: nginx
  rules:
    - host: oidc.patch.stplabs.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: authelia-theme-proxy
                port:
                  number: 80
  tls:
    - hosts:
        - oidc.patch.stplabs.io
      secretName: authelia-tls
